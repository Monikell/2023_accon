getwd()
install.packages("plyr")
install.packages("dplyr")
install.packages("ggplot2")
library(plyr)
library(dplyr)
library(ggplot2)
# data enter
evan_data <- read.csv(file = "TXeco_data_CN rates for plants.csv")
View(evan_data)
# see column names
colnames(evan_data)
## C and N means, per site here:
c_means <- ddply(evan_data, .(site, NCRS.code),
transform, mean.c.value = mean(c.leaf))
CN_means <- ddply(c_means, .(site, NCRS.code),
transform, mean.n.value = mean(n.leaf))
## C and N Mean for species here:
c_means_species <- ddply(evan_data, .(site, NCRS.code),
transform, mean.c.value = mean(c.leaf))
CN_means_species <- ddply(c_means_species, .(NCRS.code),
transform, mean.n.value = mean(n.leaf))
## C and N Mean for species here:
c_means_species <- ddply(evan_data, .(NCRS.code),
transform, mean.c.value = mean(c.leaf))
CN_means_species <- ddply(c_means_species, .(NCRS.code),
transform, mean.n.value = mean(n.leaf))
View(CN_means_species)
## C and N Mean for species only here:
c_means_species <- ddply(evan_data, .(NCRS.code),
transform, mean.c.value_species = mean(c.leaf))
CN_means_species <- ddply(c_means_species, .(NCRS.code),
transform, mean.n.value_species = mean(n.leaf))
colnames(evan_data)
View(evan_data)
# data enter
evan_data <- read.csv(file = "TXeco_data_CN rates for plants.csv")
# Dataset ID, Observation and AccSp. ID and name, lat long and N and C values
data_clean <- evan_data[c(1:4,6,8:12,15:16)]
colnames(data_clean)
View(evan_data)
# Getting the mean N and C for each species and site:
c_means <- ddply(data_clean, .(site, NCRS.code),
transform, mean.c.value = mean(c.leaf))
View(c_means)
# Getting the mean N and C for each species and site:
c_means <- ddply(data_clean, .(site, NCRS.code),
transform, mean.c.value_persite = mean(c.leaf))
colnames(c_mean)
colnames(c_means)
# Getting the mean N and C for each species and site:
c_means_site <- ddply(data_clean, .(site, NCRS.code),
transform, mean.c.value_persite = mean(c.leaf))
remove(c_means)
View(c_means_site)
cn_means_site <- ddply(c_means_site, .(site, NCRS.code),
transform, mean.n.value_persite = mean(n.leaf))
View(cn_means_site)
colnames(cn_means_site)
c_means_taxon <- ddply(data_clean, .(NCRS.code),
transform, mean.c.value_taxon = mean(c.leaf))
View(c_means_taxon)
c_means_taxon <- ddply(cn_means_site, .(NCRS.code),
transform, mean.c.value_taxon = mean(c.leaf))
View(c_means_taxon)
# Adding the mean N to the C mean per species to make one document
cn_means_taxon <- ddply(c_means_taxon, .(NCRS.code),
transform, mean.n.value_taxon = mean(n.leaf))
colnames(cn_means_taxon)
# I think it worked, writing to csv.
write.csv(cn_means_taxon, file = "evan_tx_data_cn_means_mkelley")
# I think it worked, writing to csv.
write.csv(cn_means_taxon, file = "evan_tx_data_cn_means_mkelley.csv")
# inserting taxon information
taxon_info <- read.csv(file = "species_code_taxon.csv")
# Adding taxon informaiton?
test <- merge(taxon_info, data_clean)
View(test)
# inserting taxon information
taxon_info <- read.csv(file = "species_code_taxon.csv")
View(taxon_info)
# Adding taxon informaiton?
test <- merge(taxon_info, data_clean)
View(test)
?"merge"
# Adding taxon informaiton?
test <- merge(taxon_info, data_clean, all = TRUE)
View(test)
# data enter
evan_data <- read.csv(file = "TXeco_data_CN rates for plants.csv")
# see column names
colnames(evan_data)
# Dataset ID, Observation and AccSp. ID and name, lat long and N and C values
data_clean <- evan_data[c(1:4,6,8:12,15:16)]
View(evan_data)
colnames(data_clean)
# Dataset ID, Observation and AccSp. ID and name, lat long and N and C values
data_clean <- evan_data[c(1:4,6,8:13,15:17)]
colnames(data_clean)
# inserting taxon information
taxon_info <- read.csv(file = "species_code_taxon.csv")
# inserting taxon information
taxon_info <- read.csv(file = "species_code_taxon.csv")
# Adding taxon informaiton?
test <- merge(taxon_info, data_clean, all = TRUE)
View(test)
unique(taxon_info$NCRS.code)
count(unique(taxon_info$NCRS.code))
count(taxon_info$NCRS.code)
one<- unique(taxon_info$NCRS.code)
count(one)
count(one)
View(test)
# Getting the mean N and C for each species and site:
c_means_site <- ddply(data_clean, .(site, NCRS.code),
transform, mean.c.value_persite = mean(c.leaf))
# Getting the mean N and C for each species and site:
c_means_site <- ddply(test, .(site, NCRS.code),
transform, mean.c.value_persite = mean(c.leaf))
# Getting means for N, and combining it with "c_means_site"
cn_means_site <- ddply(c_means_site, .(site, NCRS.code),
transform, mean.n.value_persite = mean(n.leaf))
# Getting the mean N and C for each species and site:
c_means_site <- ddply(test, .(site, NCRS.code),
transform, mean.c.value_persite = mean(c.leaf))
# Getting means for N, and combining it with "c_means_site"
cn_means_site <- ddply(c_means_site, .(site, NCRS.code),
transform, mean.n.value_persite = mean(n.leaf))
colnames(cn_means_site)
# Getting the mean C per species
c_means_taxon <- ddply(cn_means_site, .(NCRS.code),
transform, mean.c.value_taxon = mean(c.leaf))
# Adding the mean N to the C mean per species to make one document
cn_means_taxon <- ddply(c_means_taxon, .(NCRS.code),
transform, mean.n.value_taxon = mean(n.leaf))
colnames(cn_means_taxon)
View(cn_means_taxon)
# I think it worked, writing to csv.
write.csv(cn_means_taxon, file = "evan_tx_data_cn_means_mkelley.csv")
